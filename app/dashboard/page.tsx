'use client'; import {useEffect,useState} from 'react'; import {browserSupabase} from '../../lib/supabase/browser'; import {adminRepository} from '../../lib/data/admin-repository';
const nav=[['products','Products'],['categories','Categories'],['inventory','Inventory'],['orders','Orders'],['customers','Customers'],['banners','Banners'],['featured','Featured'],['settings','Settings']];
export default function Dashboard(){const[stats,setStats]=useState<Record<string,number>>({});const[error,setError]=useState('');useEffect(()=>{adminRepository.overview().then(setStats).catch(e=>setError(e.message))},[]);return <main className="min-h-screen p-5 md:p-10"><header className="mb-10 flex items-center justify-between"><div><div className="text-2xl font-black text-leaf-700">mini group</div><h1 className="mt-2 text-3xl font-black">Operations dashboard</h1><p className="text-slate-500">Duhok fresh grocery marketplace</p></div><button onClick={()=>browserSupabase().auth.signOut().then(()=>location.href='/login')} className="rounded-xl border border-leaf-100 bg-white px-4 py-2">Sign out</button></header>{error&&<div className="mb-6 rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[['Products','products'],['Categories','categories'],['Customers','customers'],['Orders','orders'],['Sales IQD','sales_iqd']].map(([label,key])=><div className="glass rounded-3xl p-6" key={key}><p className="text-sm text-slate-500">{label}</p><strong className="mt-3 block text-3xl text-leaf-700">{stats[key]??'—'}</strong></div>)}</section><section className="mt-8 glass rounded-3xl p-6"><h2 className="text-xl font-bold">Manage Mini group</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{nav.map(([key,label])=><a href={`/dashboard/${key}`} key={key} className="rounded-2xl bg-leaf-50 p-5 font-bold text-leaf-700 transition hover:-translate-y-1">{label} <span className="float-right">→</span></a>)}</div></section></main>}
