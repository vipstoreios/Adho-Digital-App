'use client';
import {FormEvent,useState} from 'react'; import {supabase} from '../../lib/supabase';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const {error}=await supabase().auth.signInWithPassword({email,password});if(error)setError(error.message);else location.href='/dashboard';setBusy(false)}return <main className="min-h-screen grid place-items-center p-6 bg-gradient-to-br from-brand-50 via-white to-emerald-100"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-8"><div className="mb-8"><div className="text-3xl font-black text-brand-700">mini group</div><p className="mt-2 text-slate-500">Fresh grocery management</p></div><h1 className="text-2xl font-bold">Admin sign in</h1><div className="mt-6 space-y-4"><input required type="email" placeholder="Admin email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-2xl border border-emerald-100 bg-white/70 p-4 outline-none"/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-2xl border border-emerald-100 bg-white/70 p-4 outline-none"/></div>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}<button disabled={busy} className="mt-6 w-full rounded-2xl bg-gradient-to-r from-brand-700 to-brand-500 p-4 font-bold text-white disabled:opacity-50">{busy?'Signing in…':'Sign in securely'}</button></form></main>}
